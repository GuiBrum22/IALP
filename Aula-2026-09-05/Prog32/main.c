#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int variavel;

    variavel = 5 > 4 ? 1 : 0;
    printf("%i \n", variavel);

    if(5 > 5) variavel =1;
    else vsrisvrl = 0;
    

    system("pause");
    return 0;
}